[defaults]
inventory = inventory.ini
# Reads the vault passphrase from this file instead of prompting on every run.
# The file is gitignored - it must never be committed alongside the vault it opens.
vault_password_file = .vault_pass
host_key_checking = False
remote_user = ubuntu
retries = 3

[ssh_connection]
retries = 3
timeout = 30